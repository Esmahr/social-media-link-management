import { UppercaseSocialMediaNamePipe } from './uppercase-social-media-name.pipe';

describe('UppercaseSocialMediaNamePipe', () => {
  it('create an instance', () => {
    const pipe = new UppercaseSocialMediaNamePipe();
    expect(pipe).toBeTruthy();
  });
});
